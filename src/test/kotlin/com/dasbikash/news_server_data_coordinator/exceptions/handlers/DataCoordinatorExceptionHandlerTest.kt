package com.dasbikash.news_server_data_coordinator.exceptions.handlers

import com.dasbikash.news_server_data_coordinator.exceptions.HighestLevelException
import org.junit.jupiter.api.AfterEach
import org.junit.jupiter.api.BeforeEach

import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

internal class DataCoordinatorExceptionHandlerTest {

    @BeforeEach
    fun setUp() {
    }

    @AfterEach
    fun tearDown() {
    }
    @Test
    fun highestLevelErrorTesting(){
        /*val nums = listOf<Int>()

        try {
            println(nums.get(10))
        }catch (ex:Exception){
            ex.printStackTrace()
            DataCoordinatorExceptionHandler.handleException(HighestLevelException(ex))
        }
        Thread.sleep(10000)*/
    }
}